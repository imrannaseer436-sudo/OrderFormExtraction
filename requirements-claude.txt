anthropic>=0.68.0     # Claude API SDK
pydantic>=2.0          # already required by the Ollama pipeline (schema.py) -- shared
Pillow>=10.0.0         # already required by the Ollama pipeline -- shared; used here only to
                        # downscale a photo if it exceeds the model's high-res cap
python-dotenv>=1.0.0   # loads ANTHROPIC_API_KEY (and SERVER/DB/USER/PASSWORD) from .env
pyodbc>=5.0.0           # brandlist_match.py -- local (free) cross-check against the SQL Server
                        # product catalog; needs "ODBC Driver 17 for SQL Server" installed
rapidfuzz>=3.0.0        # fuzzy item-name matching in brandlist_match.py
